#!/bin/bash 
echo "Hello World!"

# "#!"" is called "Shebang".
# Everything after "#!/bin/" is the interpreter. 
# The bash interpreter is considered to be a terminal instance.
# The shell interpreter is the shell, the interpreter of the commands.
# There are other interpreters available (e.g. #!/bin/python)